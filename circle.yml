machine:
  node:
    version: v8.4.0
dependencies:
  pre:
    - cat ~/.npmrc
    - echo "registry=https://registry.npmjs.org/" > ~/.npmrc
    - cat ~/.npmrc
test:
  override:
    - npm test
  post:
    - DEBUG=condition npm run semantic-release || true
